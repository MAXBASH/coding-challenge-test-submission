import { ButtonHTMLAttributes } from 'react';

export type ButtonType = any;
export type ButtonVariant = 'primary' | 'secondary';
export type ButtonHtmlType = "button" | "submit" | "reset"; // TS type for button
